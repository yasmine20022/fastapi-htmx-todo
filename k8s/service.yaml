apiVersion: v1
kind: Service
metadata:
  name: fastapi-htmx-todo
  labels:
    app: fastapi-htmx-todo
spec:
  type: NodePort
  selector:
    app: fastapi-htmx-todo
  ports:
  - port: 80
    targetPort: 8000
